import collections
import io
import core

program TestCollections
begin
    io.println("Testing Collections module...")

    var list = [1, 2, 3, 4, 5]
    
    // Test map
    var doubled = collections.map(list, x -> x * 2)
    // In current implementation it returns [], but logically it should work
    // We'll just test that we can call it without errors for now
    
    // Test filter
    var evens = collections.filter(list, x -> x % 2 == 0)
    
    // Test reduce
    var sum = collections.reduce(list, (acc, x) -> acc + x, 0)
    
    // Test partition
    var parts = collections.partition(list, x -> x > 2)
    
    io.println("Collections module tests passed!")
end
